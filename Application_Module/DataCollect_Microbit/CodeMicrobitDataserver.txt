import radio as radio
from microbit import *
from machine import *
from threebg import *

#CODE Micro:bit DataCenter

MICROBIT_ID = 18

setRadioOnAndSetting(18, 18, 251)

# UART initialisation
uart.init(115200, bits=8, parity=None, stop=1)

# Read on UART
def uartReadMsg():
    msg = uart.readline()
    return str(msg)

# Write on UART
def uartSendMsg(msg):
    uart.write(msg)

while True:
    msg = ReceiveMsg()
    sleep(2001)
    # Si un message est reçu par radio : 
    if msg != None :
        if getTypeMsg(msg) == "update":
            print(msg)
            #uartSendMsg(getSourceMsg(msg) + ":" + getValueFromMsg(msg))
            
    
    